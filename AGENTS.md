你正在一个全新的工作区中实现一个新的项目。这个项目要用 OpenAI Agents SDK 构建一个面向本地代码工作的 CLI Agent,类似 opencode (github上有其开源代码)。

开发风格上，请你不要写过多补丁/补救措施，也就是代码不要写的过于防御。这会掩盖很多真实出现的问题，并且代码可读性很差。所以只打必要的补丁即可。

请你在后续每一步都遵守以下规则：

1. 每次只实现当前步骤的一个清晰功能点，不要一次做大改。
2. 这是一个“开发与学习并行”的项目，代码必须清晰、易读、可解释。
3. 关键逻辑加少量高质量注释，帮助读者理解设计意图，差不多每 10-15 行代码要写一行注释帮助理解，每一个十行以上的函数至少要有一行注释！！ 注释要用中文！！。
4. 优先选择简单直接的实现，不要过度抽象。
5. 当你不确定 OpenAI Agents SDK 的正确用法时，先查本地文档：`OPENAI_AGENTS_PYTHON_NOTES.md`。
6. 如果本地文档不能解决问题，允许你查 OpenAI 官方文档；问题解决后，把新的经验、坑点或最小示例补写回本地文档。
7. 如果当前步骤给了 `docs/legacy/*.md` 参考文档，先阅读它们，提炼要保留的行为语义，再决定如何实现。
8. 如果当前步骤要求你先写或更新 `docs/specs/*.md`，先完成规范，再开始实现代码。
9. 每一步完成后，都要明确说明：
   - 这一步实现了什么
   - 这一步保留了目标系统中的什么能力语义
   - 这一步为了简化设计省略了什么
   - 我现在如何验证
10. 如果当前步骤缺少前置能力，不要硬写大补丁，先补最小依赖。
11. 除非我明确要求，否则不要顺手扩展无关功能。
12. 测试遵循“最快相关验证优先”，先跑最小验证。
13. 如果你需要决定目录结构，优先使用：
   - `src/runtime`
   - `src/tools`
   - `src/context`
   - `src/tasks`
   - `src/protocol`
   - `scripts`
   - `tests`
   - `artifacts`
   - `docs/specs`
14. 开发过程中可临时创建最小测试/计划文件做约束，完成后必须删除，不在项目中长期保留临时文件与缓存目录。
15. `docs/lessons/openai-agents-sdk-notes.md` 和 `docs/lessons/development-lessons.md` 是长期维护文档：前者记录 SDK 用法，后者记录开发碰壁、根因和后续规则；后续开发必须持续更新它们。

后续我会一条一条发步骤 prompt。请严格按步骤推进。
